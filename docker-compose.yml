version: '3.8'

services:
  discord-bump-bot:
    build: .
    container_name: discord-bump-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      # Use named volume for bump data persistence
      - bump-data:/app
    environment:
      - NODE_ENV=production

volumes:
  bump-data:
